name: CI
on:
  push:
    branches:
    - main
    tags:
    - "v*"
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        SCALA_VERSION: ["2.12.21", "2.13.18"]
    name: Test (Scala ${{ matrix.SCALA_VERSION }})
    steps:
    - uses: actions/checkout@v7
    - uses: coursier/cache-action@v8
    - name: Compile
      run: |
        SCALA_SEGMENT=${SCALA_VERSION//./_}
        ./mill dataClass.$SCALA_SEGMENT.test.testForked
        ./mill test.testProjV1.$SCALA_SEGMENT.compile
        ./mill test.testProjV2.$SCALA_SEGMENT.compile
        ./mill test.userProjV1.$SCALA_SEGMENT.compile
      env:
        SCALA_VERSION: ${{ matrix.SCALA_VERSION }}

  mima:
    runs-on: ubuntu-latest
    name: Binary compatibility
    steps:
    - uses: actions/checkout@v7
      with:
        fetch-depth: 0
    - uses: coursier/cache-action@v8
    - name: Check binary compatibility
      run: ./mill __.mimaReportBinaryIssues

  fmt:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: coursier/cache-action@v8
    - uses: coursier/setup-action@v3.0.0
      with:
        apps: scalafmt
    - name: Check formatting
      run: scalafmt --test

  publish:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7
    - uses: coursier/cache-action@v8
    - name: Release
      run: |
        if [[ "$GITHUB_REF" == refs/tags/* ]]; then
          ./mill -i mill.scalalib.SonatypeCentralPublishModule/ --bundleName "data-class-${GITHUB_REF#refs/tags/v}"
        else
          for tag in $(git tag --points-at HEAD); do
            git tag -d "$tag"
          done
          ./mill -i mill.scalalib.SonatypeCentralPublishModule/
        fi
      env:
        MILL_PGP_SECRET_BASE64: ${{ secrets.PUBLISH_SECRET_KEY }}
        MILL_PGP_PASSPHRASE: ${{ secrets.PUBLISH_SECRET_KEY_PASSWORD }}
        MILL_SONATYPE_USERNAME: ${{ secrets.PUBLISH_USER }}
        MILL_SONATYPE_PASSWORD: ${{ secrets.PUBLISH_PASSWORD }}
